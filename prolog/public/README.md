# public/ — Web Frontend Assets

Static HTML/CSS/JS for the interactive exploration frontend.
Served by `working_server.pl` in the parent directory.

## Files

| File | Purpose |
|------|---------|
| `index.html` | Main page |
| `app.js` | Frontend logic — sends queries to Prolog HTTP backend |
| `styles.css` | Styling |

## Status

Half-built. The backend (`working_server.pl`) functions but the frontend
needs work for Phase 3 (Interactive Exploration). See TODOS.md P3-1.
